<%= form_for(@membre) do |f| %>
  <% if @membre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membre.errors.count, "error") %> prohibited this membre from being saved:</h2>

      <ul>
      <% @membre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pseudo %><br />
    <%= f.text_field :pseudo %>
  </div>
  <div class="field">
    <%= f.label :passwd %><br />
    <%= f.text_field :passwd %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
