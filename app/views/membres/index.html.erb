<div class="page-header">
  <h1>Tous les vanneurs <small>Même ceux que vous ne connaissez pas encore</small></h1>
</div>


<div class="row-fluid">
  <div class="span6">
  <% @membres.values_at(* @membres.each_index.select {|i| i.even?}).each do |v| %>
    <%= render :partial => "membres/membre", :locals => {:membre => v} %>
  <% end %>
  </div>
  
  <div class="span6">
  <% @membres.values_at(* @membres.each_index.select {|i| i.odd?}).each do |v| %>
    <%= render :partial => "membres/membre", :locals => {:membre => v} %>
  <% end %>
  </div>
</div>