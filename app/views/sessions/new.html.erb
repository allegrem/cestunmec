<div id="main_frame">

<h2>Connexion</h2>

<% if flash[:alert] %>
  <p id="notice" ><%= flash[:alert] %></p>
<% end %>

<%= form_tag do %>
  <div>
    <label for="pseudo" >Pseudo :</label>
    <%= text_field_tag :pseudo, params[:pseudo], :autofocus => "autofocus" %>
  </div>
  
  <div>
    <label for="passwd" >Mot de passe :</label>
    <%= password_field_tag :passwd, params[:passwd] %>
  </div>
  
  <div>
    <%= submit_tag "Go go go !!" %>
  </div>
<% end %>


<p>
  <strong>Pas encore inscrit ?</strong> Rejoins vite la première communauté de vanneurs en France grâce à <%= link_to "notre formulaire d'inscription", new_membre_path %> super simple !
</p>

</div>